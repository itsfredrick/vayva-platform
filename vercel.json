{
  "version": 2,
  "buildCommand": "pnpm turbo run build --filter=web...",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web packages infra",
  "github": {
    "silent": true
  }
}